---
title: Типы данных и переменные
description: "Структура проекта Go: ограничение одной функции main() на пакет, организация примеров в отдельные каталоги, запуск конкретных файлов через go run, управление множественными entry points."
keywords: "структура проекта go, main функция golang, go run команда, организация файлов go, множественные main golang, package main go"
lang: ru
parent: Базовые типы и переменные
nav_order: 2
has_toc: false
---

### Типы данных и переменные — структура проекта

**Ключевая проблема:** В Go может быть только **одна функция `main()`** на весь проект. Несколько файлов с `main()` в одном каталоге вызовут ошибку компиляции.

**Решение:** Создавайте отдельные каталоги для каждого примера:

```
src/
├── day02/
│   ├── data-types/
│   │   └── main.go
│   └── variables/
│       └── main.go
```

**Запуск конкретного примера:**
```bash
cd src/day02/data-types
go run .
```
